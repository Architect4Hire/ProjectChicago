import type { ReactNode } from "react";
import { Button } from "./Button";
export function Spinner({ label = "Loading" }: { label?: string }) { return <span className="inline-flex items-center gap-2 text-sm text-gray-500 dark:text-gray-400" role="status"><span className="size-5 animate-spin rounded-full border-2 border-brand-500/25 border-t-brand-500" aria-hidden="true" />{label}</span>; }
export function Skeleton({ className = "h-4 w-full" }: { className?: string }) { return <span className={`block animate-pulse rounded-md bg-gray-200 dark:bg-gray-800 ${className}`} aria-hidden="true" />; }
export function EmptyState({ title, description, icon, action }: { title: string; description: string; icon?: ReactNode; action?: ReactNode }) { return <div className="flex min-h-52 flex-col items-center justify-center rounded-xl border border-dashed border-gray-300 p-8 text-center dark:border-gray-700">{icon}<h3 className="mt-3 font-semibold text-gray-900 dark:text-white">{title}</h3><p className="mt-1 max-w-sm text-sm text-gray-500 dark:text-gray-400">{description}</p>{action && <div className="mt-5">{action}</div>}</div>; }
export function ErrorState({ retry }: { retry?: () => void }) { return <EmptyState title="Something went wrong" description="We couldn't load this content. Try again, or contact support if the problem continues." action={retry && <Button variant="outline" onClick={retry}>Try again</Button>} />; }
